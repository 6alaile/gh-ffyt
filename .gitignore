# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.*

# Package build / install artifacts
build/
dist/
*.egg-info/
.eggs/
*.whl

# Node
node_modules/
.npm/
.eslintcache

# Spec-driven pipeline outputs (everything goes under build/)
build/
specs/_draft.json

# YouTube credentials (NEVER commit)
client_secrets.json
youtube_token.pickle
*_token.pickle
*_client_secrets.json

# Parked legacy files during the MD2YT layout refactor
# (scripts/, templates/, hf/, requirements.txt live here until the
# follow-up commit deletes them; the new layout under src/pipeline/
# is the source of truth from then on.)
tmp/

# Package install metadata (regenerated by pip)
*.egg-info/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Build artifacts
*.tsbuildinfo
.cache/
