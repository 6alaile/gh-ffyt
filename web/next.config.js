/** @type {import('next').NextConfig} */
const nextConfig = {
  // The whole web/ subtree is the Vercel project root. The Python pipeline
  // (src/pipeline/) is unaffected — Vercel only sees this directory.
  reactStrictMode: true,
};

module.exports = nextConfig;